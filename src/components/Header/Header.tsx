import { IonHeader, IonTitle, IonToolbar } from '@ionic/react'

const Header = () => {
    return (
        <IonHeader>
            <IonToolbar>
                <IonTitle>Select option</IonTitle>
            </IonToolbar>
        </IonHeader>
    )
}

export default Header